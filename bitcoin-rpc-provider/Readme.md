# Bitcoin RPC Provider

Implementation of the `Wallet` and `Blockchain` traits from the [dlc-manager](../dlc-manager) using the bitcoind RPC.
It is mainly a wrapper around the [rust-bitcoincore-rpc](https://github.com/rust-bitcoin/rust-bitcoincore-rpc) crate.
